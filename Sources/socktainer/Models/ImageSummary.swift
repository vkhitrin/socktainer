//
// ImageSummary.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

internal struct ImageSummary: Sendable, Content, Hashable {

    /** ID is the content-addressable ID of an image.  This identifier is a content-addressable digest calculated from the image's configuration (which includes the digests of layers used by the image).  Note that this digest differs from the `RepoDigests` below, which holds digests of image manifests that reference the image.  */
    internal var id: String
    /** ID of the parent image.  Depending on how the image was created, this field may be empty and is only set for images that were built/created locally. This field is empty if the image was pulled from an image registry.  */
    internal var parentId: String
    /** List of image names/tags in the local image cache that reference this image.  Multiple image tags can refer to the same image, and this list may be empty if no tags reference the image, in which case the image is \"untagged\", in which case it can still be referenced by its ID.  */
    internal var repoTags: [String]
    /** List of content-addressable digests of locally available image manifests that the image is referenced from. Multiple manifests can refer to the same image.  These digests are usually only available if the image was either pulled from a registry, or if the image was pushed to a registry, which is when the manifest is generated and its digest calculated.  */
    internal var repoDigests: [String]
    /** Date and time at which the image was created as a Unix timestamp (number of seconds since EPOCH).  */
    internal var created: Int
    /** Total size of the image including all layers it is composed of.  */
    internal var size: Int64
    /** Total size of image layers that are shared between this image and other images.  This size is not calculated by default. `-1` indicates that the value has not been set / calculated.  */
    internal var sharedSize: Int64
    /** User-defined key/value metadata. */
    internal var labels: [String: String]
    /** Number of containers using this image. Includes both stopped and running containers.  `-1` indicates that the value has not been set / calculated.  */
    internal var containers: Int
    /** Manifests is a list of manifests available in this image. It provides a more detailed view of the platform-specific image manifests or other image-attached data like build attestations.  WARNING: This is experimental and may change at any time without any backward compatibility.  */
    internal var manifests: [ImageManifestSummary]?
    internal var descriptor: OCIDescriptor?

    internal init(
        id: String, parentId: String, repoTags: [String], repoDigests: [String], created: Int, size: Int64, sharedSize: Int64, labels: [String: String], containers: Int,
        manifests: [ImageManifestSummary]? = nil, descriptor: OCIDescriptor? = nil
    ) {
        self.id = id
        self.parentId = parentId
        self.repoTags = repoTags
        self.repoDigests = repoDigests
        self.created = created
        self.size = size
        self.sharedSize = sharedSize
        self.labels = labels
        self.containers = containers
        self.manifests = manifests
        self.descriptor = descriptor
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case id = "Id"
        case parentId = "ParentId"
        case repoTags = "RepoTags"
        case repoDigests = "RepoDigests"
        case created = "Created"
        case size = "Size"
        case sharedSize = "SharedSize"
        case labels = "Labels"
        case containers = "Containers"
        case manifests = "Manifests"
        case descriptor = "Descriptor"
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var keyedContainer = encoder.container(keyedBy: CodingKeys.self)
        try keyedContainer.encode(id, forKey: .id)
        try keyedContainer.encode(parentId, forKey: .parentId)
        try keyedContainer.encode(repoTags, forKey: .repoTags)
        try keyedContainer.encode(repoDigests, forKey: .repoDigests)
        try keyedContainer.encode(created, forKey: .created)
        try keyedContainer.encode(size, forKey: .size)
        try keyedContainer.encode(sharedSize, forKey: .sharedSize)
        try keyedContainer.encode(labels, forKey: .labels)
        try keyedContainer.encode(containers, forKey: .containers)
        try keyedContainer.encodeIfPresent(manifests, forKey: .manifests)
        try keyedContainer.encodeIfPresent(descriptor, forKey: .descriptor)
    }
}
