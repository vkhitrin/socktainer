//
// ContainerState.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import Vapor

/// ContainerState stores container&#39;s running state. It&#39;s part of ContainerJSONBase and will be returned by the \&quot;inspect\&quot; command.
internal struct ContainerState: Sendable, Content, Hashable {

    internal enum Status: String, Sendable, Content, Hashable, CaseIterable {
        case created = "created"
        case running = "running"
        case paused = "paused"
        case restarting = "restarting"
        case removing = "removing"
        case exited = "exited"
        case dead = "dead"
    }
    /** String representation of the container state. Can be one of \"created\", \"running\", \"paused\", \"restarting\", \"removing\", \"exited\", or \"dead\".  */
    internal var status: Status?
    /** Whether this container is running.  Note that a running container can be _paused_. The `Running` and `Paused` booleans are not mutually exclusive:  When pausing a container (on Linux), the freezer cgroup is used to suspend all processes in the container. Freezing the process requires the process to be running. As a result, paused containers are both `Running` _and_ `Paused`.  Use the `Status` field instead to determine if a container's state is \"running\".  */
    internal var running: Bool?
    /** Whether this container is paused. */
    internal var paused: Bool?
    /** Whether this container is restarting. */
    internal var restarting: Bool?
    /** Whether a process within this container has been killed because it ran out of memory since the container was last started.  */
    internal var oOMKilled: Bool?
    internal var dead: Bool?
    /** The process ID of this container */
    internal var pid: Int?
    /** The last exit code of this container */
    internal var exitCode: Int?
    internal var error: String?
    /** The time when this container was last started. */
    internal var startedAt: String?
    /** The time when this container last exited. */
    internal var finishedAt: String?
    internal var health: Health?

    internal init(
        status: Status? = nil, running: Bool? = nil, paused: Bool? = nil, restarting: Bool? = nil, oOMKilled: Bool? = nil, dead: Bool? = nil, pid: Int? = nil, exitCode: Int? = nil,
        error: String? = nil, startedAt: String? = nil, finishedAt: String? = nil, health: Health? = nil
    ) {
        self.status = status
        self.running = running
        self.paused = paused
        self.restarting = restarting
        self.oOMKilled = oOMKilled
        self.dead = dead
        self.pid = pid
        self.exitCode = exitCode
        self.error = error
        self.startedAt = startedAt
        self.finishedAt = finishedAt
        self.health = health
    }

    internal enum CodingKeys: String, CodingKey, CaseIterable {
        case status = "Status"
        case running = "Running"
        case paused = "Paused"
        case restarting = "Restarting"
        case oOMKilled = "OOMKilled"
        case dead = "Dead"
        case pid = "Pid"
        case exitCode = "ExitCode"
        case error = "Error"
        case startedAt = "StartedAt"
        case finishedAt = "FinishedAt"
        case health = "Health"
    }

    // Encodable protocol methods

    internal func encode(to encoder: Encoder) throws {
        var keyedContainer = encoder.container(keyedBy: CodingKeys.self)
        try keyedContainer.encodeIfPresent(status, forKey: .status)
        try keyedContainer.encodeIfPresent(running, forKey: .running)
        try keyedContainer.encodeIfPresent(paused, forKey: .paused)
        try keyedContainer.encodeIfPresent(restarting, forKey: .restarting)
        try keyedContainer.encodeIfPresent(oOMKilled, forKey: .oOMKilled)
        try keyedContainer.encodeIfPresent(dead, forKey: .dead)
        try keyedContainer.encodeIfPresent(pid, forKey: .pid)
        try keyedContainer.encodeIfPresent(exitCode, forKey: .exitCode)
        try keyedContainer.encodeIfPresent(error, forKey: .error)
        try keyedContainer.encodeIfPresent(startedAt, forKey: .startedAt)
        try keyedContainer.encodeIfPresent(finishedAt, forKey: .finishedAt)
        try keyedContainer.encodeIfPresent(health, forKey: .health)
    }
}
